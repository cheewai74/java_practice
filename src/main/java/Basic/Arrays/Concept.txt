
ArrayList class manages a sequence of objects.
ArrayList<T> collects objects of type T.

To treat primitive type values as objects, you must use wrapper class.

for (Type variable : collection)
    statement

 Use the System.arraycopy method to copy elements from one array to another.